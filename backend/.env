# MongoDB connection string — replace with your Atlas URI or local URI
MONGO_URI=mongodb://localhost:27017/merrykids

# JWT secret — change this to any long random string
JWT_SECRET=merrykids_super_secret_2026

# Port the server runs on
PORT=5000
